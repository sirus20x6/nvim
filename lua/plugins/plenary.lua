-- ~/.config/nvim/lua/plugins/plenary.lua
return {
    'nvim-lua/plenary.nvim',
    config = function()
        -- You can add any specific configuration for plenary here if needed
        -- Since plenary is mainly a utility library, it usually doesn't require much configuration
    end
}
